// 이 파일은 packages/design-system/products.mjs 에서 생성되었다.
// 직접 편집하지 말고 정본을 고친 뒤 저장소 루트에서
// `npm run design-system:sync` 를 실행한다.
import { GitCompareArrows } from 'lucide-react';
export const PRODUCT = {
  "id": "config-diff-viewer",
  "name": "Config Diff Viewer",
  "description": "설정 파일의 차이를 비교합니다."
} as const;
export const ProductIcon = GitCompareArrows;
